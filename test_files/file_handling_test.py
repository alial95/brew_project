import unittest 
from classes.person import Person

def people():
    return [Person(name, age)
            for name, age in (
                ('steve', 24,
                ('sally', 76,
            ]

